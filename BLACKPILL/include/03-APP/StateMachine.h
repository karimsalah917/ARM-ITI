#pragma once

/**
 * @brief Initialize the traffic light system.
 *
 * This function initializes the traffic light system by configuring any necessary setup, including initializing the drivers from lower layers.
 * Example usage:
 * 
 * TrafficLight_Init();
 */
void TrafficLight_Init();

